{
  "app": "npx ts-node --prefer-ts-exts bin/app.ts",
  "watch": {
    "include": ["**"],
    "exclude": [
      "README.md",
      "cdk*.json",
      "**/*.d.ts",
      "**/*.js",
      "tsconfig.json",
      "package*.json",
      "yarn.lock",
      "node_modules",
      "test"
    ]
  },
  "build": "npm run build",
  "context": {
    // Disable CLI version check noise
    "@aws-cdk/core:enableStackNameDuplicates": true,
    "@aws-cdk/aws-iam:minimizePolicies": true,
    // Use new-style bootstrap (CDKv2 default)
    "@aws-cdk/core:bootstrapQualifier": "hnb659fds",
    // S3 SSL requests only
    "@aws-cdk/aws-s3:serverAccessLogsUseBucketPolicy": true,
    // ECS optimisation
    "@aws-cdk/aws-ecs:disableExplicitDeploymentControllerForCircuitBreaker": false,
    // Default stage — override with: cdk deploy -c stage=staging
    "stage": "prod"
  }
}
